You're building the sustainability-tracking half of Carbo, a cross-agent model
  gateway. Nico owns the gateway (already on main); you own the metrics + display.
  Your work connects to his through exactly one function, so you're never blocked
  on him — build against synthetic events.

  Setup:
    git checkout main && git pull
    git checkout -b feat/usage-tracking
    npm install

  Read these two docs first and treat tracker/TEAMMATE_TRACKER.md as your spec:
    - tracker/TEAMMATE_TRACKER.md   ← your full task list + frozen contract
    - tracker/GATEWAY_NOTES.md       ← how you integrate with Nico's gateway

  The one rule (the seam): the gateway calls recordUsage(e: UsageEvent) on every
  cacheable request, where UsageEvent = { cacheHit, tokens, model, author, ts }.
  Never rename those fields. The entire GET /api/metrics response shape is YOURS —
  expand it freely (saved vs spent, latest, etc.); the gateway never reads it.

  You own and edit:
    - lib/metrics.ts        ← overwrite the minimal baseline; keep recordUsage's signature
    - lib/sustainability.ts ← NEW: the kWh/water/CO2 conversion engine (label + cite constants)
    - app/api/metrics/route.ts
    - the Claude Code statusline adapter, the Codex turn-stop hook adapter
    - /dashboard (the ZenScreen view)

  Do NOT touch: lib/gateway/**, app/v1/**, lib/clients.ts, lib/constants.ts,
  scripts/seed.ts — those are Nico's. Scope is Claude Code + Codex only (no VS Code
  extension).

  Build in this order, and write a small synthetic UsageEvent generator first so
  you can drive everything without the live gateway:
    1. recordUsage + synthetic event generator (verify counters move)
    2. lib/sustainability.ts conversion engine (saved vs spent, cited constants)
    3. GET /api/metrics returning totals + the latest event
    4. Claude Code statusline adapter (blue water / yellow electricity)
    5. Codex turn-stop hook adapter (same conversion math, imported)
    6. /dashboard polling /api/metrics every 2s, headline water/electricity-saved
       cards, animated tick-up, sized for the ASUS ZenScreen, loading/error states
    7. swap synthetic events for the real gateway (npm run dev), then open a PR

  After each step run `npx tsc --noEmit` and `npm run lint`. Keep all conversion
  math in lib/sustainability.ts and import it into both adapters so the numbers
  always agree. Open a PR into main when it works against synthetic events.